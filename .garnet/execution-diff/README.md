This is the selected workload fixture for the Garnet execution diff lane.
The lane installs it with `npm install --prefix .garnet/execution-diff --no-package-lock` at base and head.
Editing README.md alone is the no-op proof.
