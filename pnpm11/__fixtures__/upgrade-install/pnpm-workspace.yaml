# Own workspace root: this fixture installs from the public registry, so it
# must not inherit the settings the registry-mock fixtures share.
packages: []

# The fixture keeps a dependency whose build script runs at install time, so
# an install here does the work a real install does.
allowBuilds:
  bcrypt: true
