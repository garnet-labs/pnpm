# Own workspace root: the dependencies come from the public registry, so this
# fixture must not inherit the settings the registry-mock fixtures share.
packages: []

# The point of the fixture: `chromedriver` only becomes usable once its
# install script is allowed to run and download the prebuilt binary.
allowBuilds:
  chromedriver: true
